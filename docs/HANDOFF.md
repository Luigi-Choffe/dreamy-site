# HANDOFF — estado atual e como retomar

Atualizado em 2026-08-30 (pivô para plataforma de vendas — ver bullet **PIVÔ** abaixo). Leia isto primeiro ao retomar o projeto (junto com `.agents/rules/dreamy-site.md`, `docs/PRD.md` e, para o outbound, `docs/PRD-EMAIL-OUTBOUND.md` §28–29).

## Estado

- **V1 completa e verificada** (Fases 0–7 do PRD). Fases 8–9 (DNS/host/Search Console/pós-lançamento) dependem de configuração externa — roteiro em `docs/DEPLOY.md`.
- Repositório publicado: `https://github.com/Luigi-Choffe/dreamy-site` (privado, branch `main`; identidade de commit configurada só neste repositório = conta GitHub `Luigi-Choffe`, e-mail noreply). CI (`.github/workflows/ci.yml`) **verde** no GitHub: check ≈ 1m20s + e2e ≈ 2m40s. Dependabot mensal (`.github/dependabot.yml`).
- Último `pnpm check` local **verde** (typecheck · lint · content-check · 53 testes · build); `pnpm test:e2e` 56 passed (desktop + mobile, inclui axe); Lighthouse mobile 95–98 / 100 / 100 / 100 (`docs/qa/lighthouse/`).
- **Vercel: projeto `dreamy-site` criado pelo Luigi em 2026-08-30** (time `luigichoffedremay`, `https://dreamy-site-murex.vercel.app`, deploy automático a cada push em `main`; Neon conectado). A integração desta máquina é de outra conta (`403`) — operações na Vercel são cliques do Luigi. **Desde o pivô, o projeto Vercel deste repo é a plataforma** (roteiro em `docs/DEPLOY-PLATAFORMA.md`; `docs/DEPLOY.md` fica como referência do site). Sem `NEXT_PUBLIC_SITE_ENV=production` qualquer deploy sai `noindex` (ADR-017) — e na plataforma essa variável **nunca** é definida.
- **Design pass 2026-08-18 (ADR-018)**: hero "sistema vivo" derivado do logo, `FlowDiagram` em rail/pílulas, cards de solução com subgrid alinhado, tokens de tipografia mais apertados. Skills instaladas pelo usuário em `.agents/skills/` e `.claude/skills/` (`frontend-design`, `copywriting`) — usar `frontend-design` para novas telas.
- Servidores locais parados. Para subir: `pnpm dev` (3000). E2E sobe sozinho `next start` em 3100. Verificação rápida de qualquer URL publicada: `pnpm smoke --base <url>`.
- **Outbound (2026-08-27)**: subsistema de campanhas de cold e-mail por indústria **implementado** (V1 local — `docs/PRD-EMAIL-OUTBOUND.md` §28, ADR-019…022). Comandos `pnpm outbound:*` (import/verify/campaign/arm/plan/send/sync/auto/reply/report/demo); copy-modelo + guia em `src/content/outbound/`. **Console completo** em `/interno/outbound` (`pnpm dev`): visão geral, campanha (funil + prévia da copy com lint), contatos, respostas, atividade, supressão; ações conservadoras por Server Actions (pausar/classificar/suprimir/desarmar — armar/disparar só na CLI); **modo demo**: `pnpm outbound:demo` + `/interno/outbound?demo=1` (dados simulados). Screenshots: `docs/qa/outbound-console/`. **Lote construção importado** (2026-08-27): `docs/CONTATOS/Luigi_CONSTRUCAO.xlsx` (abas Empresas/Pessoas — gitignored), 99 empresas segmentadas + 42 contatos ativos no store real, 99 aberturas personalizadas, campanha `construcao-nova-receita` em draft (mira 16 incorporadoras). Chave Resend em `.env.local`; domínio `bedreamy.com.br` verificado (DKIM/SPF). Aguardando do usuário: verificação dos 42 e-mails, `OUTBOUND_FROM`/`OUTBOUND_REPLY_TO`, DMARC, aprovação da copy, CNPJ/razão social e `outbound:arm`. **Atualização 2026-08-29**: copy aprovada (Luigi Choffe), automação **ARMADA**, 1º disparo seg 2026-08-31 09:05 pela tarefa local `DreamyOutboundAuto`; MORK criado (`.agents/mork/`, skill `/mork`).
- **PIVÔ 2026-08-29 — plataforma de vendas**: este repo deixa de ser o site institucional (duplicado e hospedado pelo sócio em outro lugar) e vira a **plataforma de vendas da Dreamy**: console `/interno/outbound` na **Vercel** (`mork.bedreamy.com.br`, `OUTBOUND_PLATFORM_ONLY=true` redireciona `/` para o console) com **login por link mágico** para o time (allowlist `OUTBOUND_TEAM_EMAILS`, cookie HMAC 30 dias — ADR-024) e **Postgres Neon** compartilhado entre o console e os CLIs do PC do Luigi (`OUTBOUND_DATABASE_URL`; sem a env, store local — ADR-023; `pnpm outbound:db migrate | push | pull | status`). Adapter Postgres e auth por link mágico já publicados (`main`, CI verde). PRD-EMAIL-OUTBOUND §29 = arquitetura + roadmap (P2 Vercel Cron, P3 webhooks/one-click, P4 respostas automáticas, P5 remoção do site). **Estado 2026-08-30 (tarde)**: itens 1–4 do `docs/DEPLOY-PLATAFORMA.md` feitos (projeto Vercel, Neon, envs + Redeploy, banco migrado com `outbound:db migrate`/`push`: 79 contatos, 99 empresas, 16 enrollments, armada SIM — CLIs do PC e console no MESMO banco; allowlist: contact@bedreamy.com.br, luigi.lgv@hotmail.com). **Falta**: login de teste do Luigi e item 5 (domínio `mork.bedreamy.com.br`).
- **CRM piloto (2026-08-30 — ADR-025)**: por ordem do Luigi, a plataforma virou o **piloto do CRM de vendas da Dreamy** (produto demonstrável e vendável; o MORK é o produto). Implementado por cima do console, com o motor de envio intocado: 7 coleções novas no store duplo (negócios, notas, tarefas, demandas, atividades, briefings, settings); **Pipeline** kanban (8 estágios; novo/contatado/respondeu derivados do outbound por `reconcileDeals`); **conta do contato** (`/interno/outbound/contatos/[id]`) com timeline unificada, notas e tarefas; aba **Hoje** (vencidas, interessados sem follow-up, semana — classificar interested cria a tarefa sozinho); **Demandas** (o time pede no console, o MORK consome com `pnpm outbound:demandas`); painel **MORK** (prestação de contas); **IA** env-gated `OUTBOUND_ANTHROPIC_API_KEY` (briefing do dia + sugestão de triagem por fetch direto, sem PII no prompt, gate humano sempre); **funil de valor** na visão geral (até reuniões, via stageHistory dos negócios); **Configuração** de marca (apresentação apenas). CLIs novas `outbound:crm` e `outbound:demandas`; o demo (`?demo=1`) conta a história completa. Time de subagentes do MORK em `.claude/agents/`: VERBO (copy), GARIMPO (leads/ICP), TRATO (respostas/CRM), FORJA (plataforma); 5ª vaga (MIRA, análise) reservada — demitir exige permissão do Luigi.
- **Aquário (2026-08-30, v2 rede neural)**: constelação viva do time em canvas (sinapses curvas + pulsos; forte = demanda real em andamento; colaboração acende quando dois agentes tocam a mesma campanha) numa lente de vidro 9:19, fixo à direita do console em telas largas (e na aba MORK nas demais): MORK no topo, 5 cadeiras (vaga = cadeira tracejada), ficha por hover/foco com função, motivo e tarefa atual derivada das demandas (src/lib/outbound/team.ts, src/app/interno/outbound/aquario.tsx) e resumo das últimas atividades no pé do tanque.
- **Agenda (2026-08-30)**: aba nova no console com o FUTURO da operação, dia a dia (10 dias): previsão da cadência rodando o MESMO computePlan do motor iterativamente (src/lib/outbound/agenda-core.ts, forecast puro e testado, rampa e idempotência inclusas), fila REAL do Resend com selo próprio, tarefas e reuniões por dia; visão geral ganhou o Stat Amanhã na cadência. Honestidade: previsão rotulada (o disparo das 09:05 decide); breaker/pending bloqueiam a previsão em vez de fingir futuro.
- **Design pass liquid glass (2026-08-30)**: o console inteiro (/interno) foi rematerializado no estilo vidro fosco claro com toques do verde do logo, sem tocar no site público: chrome próprio (ChromeGate esconde Header/Footer/cookies em /interno), escopo [data-app="console"] em globals.css redefine os tokens de superfície (todo bg-surface vira vidro com blur + fallback prefers-reduced-transparency), luz ambiente fixa da marca, navegação em barra segmentada, pílulas do fio de saúde com --warning-soft e pulso nas críticas, funis com corpo neutro e verde só na conversão, botões de fila com hierarquia (ação principal em brand-soft, reagendar em ghost), Agenda com Hoje em destaque e dots real/previsto, Pipeline em raias com header e cards elevados, login como cena centrada em vidro com SubmitButton pendente, error boundary no sistema, copy sem jargão (pending → envio(s) pendente(s); estagio → estágio). Auditoria por workflow (11 superfícies) em tool-results; espelhos antes/meio/depois no scratchpad; QA em docs/qa/outbound-console/.
- **Kanban de alto padrão + passe de usabilidade (2026-08-30)**: Pipeline reescrito como board client (pipeline/board.tsx): colunas largas (17.5rem) com scroll horizontal, fades e snap; drag and drop nativo com as regras do domínio espelhadas antes do drop (coluna abaixo do piso do outbound esmaece; perdido pede motivo e reunião pede data em diálogo); movimento otimista (useOptimistic) com toast de sucesso/erro e fallback acessível Mover para com destinos válidos em um clique. Usabilidade transversal (auditoria por workflow, 5 dimensões): pending.tsx (PendingPill/SubmitButton/ConfirmSubmit) em todos os formulários quentes (bloqueia duplo envio), supressão e cancelamento de demanda em confirmação de duas etapas na tela, pílulas do fio de saúde clicáveis (agenda/atividade/demandas), aba de campanha marca Visão geral como corrente, contatos linkados na Agenda, métricas-norte da Visão geral linkam para onde se age, triagem IA com Aplicar classe sugerida e Copiar rascunho. Pendências médias registradas e não feitas: paginação de contatos/atividade, busca na supressão, ordenação de colunas, auto-submit de filtros, atalhos de teclado, useActionState nos forms multi-campo.
- **Pergunte ao MORK (2026-08-30)**: chat com IA na lateral direita do console, abaixo do Aquário e separado por linha (shell.tsx: coluna sticky que rola por dentro; também na aba MORK para telas menores). POST /api/outbound/chat: sessão obrigatória, leitura sem lock, nada gravado; contexto SEM PII (agregados, empresas, títulos, resumos; e-mails redigidos no builder buildConsoleChatPrompt em ai.ts); responde pelo motor dual (answerConsoleQuestion, 500 tokens); demo devolve resposta canned rotulada sem IA; gate humano absoluto (o endpoint só responde, nunca age; o prompt manda apontar a aba/CLI para ações). UI aquario-chat.tsx: sugestões clicáveis, bolhas com monograma MK, indicador escrevendo, histórico só em memória da página, disclaimers de IA. Fumaça verificada: rota demo + IA real (gpt-5.4-mini com dados do Neon) respondendo correto.
- **Campanhas e Contatos refinados na Visão geral (2026-08-30)**: fim da bagunça apontada pelo Luigi: campanha real (aprovada/pausada ou com atividade) ganha card completo com régua de 4 métricas divididas por hairline (zeros esmaecidos, verde só em interessados) e barra fina de inscritos ativos; rascunhos parados colapsam em linhas compactas (Rascunhos (n)); bounce vira texto com amostra em vez de chip. Painel de Contatos virou colunas divididas por hairline vertical (total, status, verificação, top indústrias) com trilho por linha, zeros mudos e mini-barras proporcionais nas indústrias (rótulo completo, sem truncar).
- **Aba Contatos em nível superior (2026-08-31)**: quitadas as pendências da auditoria nesta aba: ordenação clicável nos cabeçalhos (?ordem=empresa|verificacao|ultimo_envio, sufixo -desc, aria-sort + seta; verificacao-desc responde quem está sujo e ultimo_envio quem esfriou), Mostrar mais no rodapé da tabela (?limite= cresce de 100 em 100 até 2000, preservando filtros/ordem; fim dos contatos inalcançáveis), filtros que aplicam sozinhos ao trocar o select (FiltroSelect client com requestSubmit; botão Filtrar segue como fallback sem JS; trocar filtro reseta o limite e preserva a ordem) e monogramas de empresa (duas iniciais, neutros) ancorando a coluna Contato; cargo/indústria truncam com tooltip. Pendências que seguem abertas nas OUTRAS abas: paginação da Atividade, busca na Supressão, atalhos de teclado, useActionState.
- **Plano de melhorias do console (2026-08-31)**: a pedido do Luigi, docs/MELHORIAS-CONSOLE.md é o contrato de qualidade do CRM piloto (P0/P1/P2 com status por item). P0 feitos no mesmo dia: fichas do Aquário viraram painel central de vidro DENTRO da lente (CSS :has por cadeira, hover e foco, aria-describedby; imune a clipping; a versão anterior era cortada pela rolagem interna da coluna, regressão introduzida junto com o chat) e a coluna lateral ganhou scrollbar fina visível (scroll-slim). Processo novo: mudança em coluna lateral/elemento flutuante exige espelho com hover aberto em 1536px e 1720px.
- **Mundos separados (2026-08-31, pedido do Luigi: menos poluição, chat maior e integrado, Aquário separado do CRM)**: a coluna lateral foi APOSENTADA. O CRM ocupa a largura toda em todas as abas; o Aquário mora só na aba MORK (a sala do time, sempre em tamanho pleno); o chat virou MorkFlutuante: botão-pílula fixo no canto inferior direito (monograma MK) presente em toda aba, abre painel de vidro fosco denso (26rem × até 55dvh, bg-white/90 + blur para legibilidade sobre texto), estado aberto persiste na sessão (sessionStorage), Esc fecha. AquarioChat ganhou prop alto (painel grande sem moldura própria). **Correção de rota no mesmo dia**: o Luigi queria a lateral de volta como ÁREA DEDICADA, não removida. Nasceu a **Sala do MORK**: aside sticky de altura da viewport (2xl+), separada do CRM por border-l + vidro contínuo (bg-white/35 + blur), com a rede neural compacta em cima (Aquario variante sala, sem rodapé), linha divisória, e o chat em ÁREA CHEIA embaixo (AquarioChat variante sala, flex-1, input ancorado no pé). O MorkFlutuante foi removido; a lente 9:19 completa segue na aba MORK.
- **Aquário VIVO (2026-08-31 — docs/AQUARIO-VIVO.md)**: o fator WOW dos agentes, pedido do Luigi (movimento, interação entre eles, inteligência quase humana; referência hub Entrada→Raciocínio→Ações). Biblioteca nova: `motion` (Framer Motion v12). Motor de conversas no canvas (aquario-rede.tsx v4): a cada ~10s um par contratado conversa — partículas com rastro indo e voltando pela sinapse + balões de vidro (AnimatePresence, mola) com falas VERDADEIRAS (`seatFalas` em team.ts: status real da cadeira + vigília fiel do cargo; MIRA vaga fica muda; regra anti-enfeite-mentiroso testada). rAF pausa com aba oculta/fora da viewport. Corpo vivo em CSS: deriva orgânica por nó (fase própria), respiração do avatar, piscada do dot, anel orbital tracejado no MORK, ficha com entrada de mola (overshoot). `seatStatus` agora fala em primeira pessoa ("Estou na demanda…", "Acabei de entregar…"). Aba MORK ganhou o palco "Como o time trabalha": fluxo Entrada de dados (contatos, respostas) → Raciocínio (lente do Aquário) → Ações automatizadas (enviados, reuniões, tarefas) em cartões de vidro com contadores de mola (useSpring + useInView) e dutos tracejados animados (mork/fluxo.tsx); empilha no mobile. Balões de nós do topo abrem para baixo-direita (não cobrem o VERBO). Espelhos: docs/qa/outbound-console/mork*.png e sala-*.png.
- **Operação com 3 campanhas (2026-09-01)**: 1º dia da construção fechou 15/15 entregues, 0 bounce, 0 complaint; 16º E1 saiu na terça. A pedido do Luigi ("Bora pode criar!"), MORK criou, e o Luigi aprovou no mesmo dia ("Pode seguir! Está aprovado!"), as campanhas `obras-sistemas-sob-medida` (17 construtoras de obra p/ terceiros, âncora Sistemas Sob Medida, tese: a margem escapa na camada canteiro↔escritório que o ERP não cobre) e `engenharia-agentes-ia` (6 serviços de engenharia, âncora Agentes de IA, tese: horas técnicas caras em tarefa que não é engenharia; agente prepara, engenheiro assina). Prova só no E3 (McKinsey 88% com fonte), zero travessão, opt-out humano no E4. Validação nova pré-inscrição: `scripts/dev/lint-campanha-alvo.ts` (92/92 renderizações limpas contra contatos reais). Aprovadas via CLI (hashes 3d4cc7eb/3cbc7a3a), 23 inscritos, 14 E1 agendados no próprio dia 1º/09 (cap 15/15); 9 restantes no ciclo de 02/09. Fornecedores (2) sem campanha por volume.
- **Redesign do console — plano (2026-09-01)**: o Luigi achou o CRM "com pouco esmero" (coisas cortando, tabelas mal formatadas, espaçamento ruim) e pediu um plano de redesign tela a tela ANTES de executar, em pedidos separados. `docs/REDESIGN-CONSOLE.md` é o contrato: auditoria com espelho full-page de 13 telas + calibração pela skill `.agents/skills/ui-ux-pro-max` (Liquid Glass validado; paleta/fontes genéricas descartadas — identidade Dreamy inegociável; density 8). 8 leis da perfumaria, 5 lotes transversais (T1 régua, T2 tabela da casa, T3 linha de lista, T4 chips/datas/plurais, T5 entrada viva) e R1–R14 por tela com defeitos OBSERVADOS e desenho alvo, mais a ordem recomendada (1 pedido = 1 lote, espelho antes/depois + gate + e2e + status na tabela). Nada implementado ainda — próximo pedido do Luigi escolhe o lote.
- **Redesign lote 1 (T1+T4, 2026-09-01)**: helpers da casa em `ui.tsx` — `plural()` (fim do "(s)"/"(ões)" em 15 pontos: fio de saúde, Aquário, Agenda, Hoje, visão geral, pipeline, conta do contato, atividade, crm-actions), `fmtQuando`/`<Quando>` (data curta dd/mm hh:mm com o carimbo completo no title — aplicada em respostas, contatos, conta, demandas, supressão, atividade, campanha e configuração) e `<TituloSecao>` (contagem esmaecida sem parênteses, `as` h2/h3 — Hoje e Demandas). Zeros mudos na Agenda (contador por dia só mostra o que existe; dia vazio sem ruído). Régua T1: cadência única `gap-8` entre seções em todas as abas (agenda/contatos/supressão subiram de 6, campanha desceu de 10). Status no REDESIGN-CONSOLE: T1 e T4 feitos.
- **Redesign lote 2 (T2+R4, 2026-09-01)**: a tabela da casa estreou em Contatos. `ContactCell` ganhou variantes — "inline" com unidades inquebráveis (nome e "· empresa" nunca mais dobram em número órfão, vale para todo o console) e "empilhada" (nome sobre empresa, truncate+title) para tabelas. `MenuLinha` novo em ui.tsx: ⋯ discreto por linha via details/summary puro (fecha ao clicar fora com backdrop CSS em globals) — o carpete de 60 botões vermelhos "Suprimir" morreu; a supressão mora no menu com ConfirmSubmit em 2 etapas ("Suprimir e-mail" → "Confirmar supressão permanente"). Campanha ativa virou chip que não quebra (truncate+title); coluna de identidade com min-w; zebra sutil em Contatos e Supressão. Status: T2 estreou (Atividade/Campanha migram em R8/R10), R4 feito.
- **Redesign lote 3 (T3+R3, 2026-09-01)**: a linha da casa estreou em Respostas. A tabela de 5 colunas com 12 formulários select+Salvar virou lista com anatomia identidade · contexto · ações: contato empilhado + chip de campanha + passo; a FALA do prospect entre aspas como centro da linha (com a Sugestão de IA como disclosure embaixo); classe atual como chip colorido + data curta + MenuLinha "⋯" com o formulário de reclassificar dentro (select+Salvar com PendingPill). Ordenação virou toggle no cabeçalho da seção ("recentes primeiro ↓"). Zebra e hover da casa. Status: T3 estreou (Hoje/Demandas/Agenda nas rodadas R1/R7/R2), R3 feito.
- **Redesign lote 4 (R2+R1, 2026-09-01)**: Agenda parou de empilhar cartões ocos — dias livres consecutivos colapsam numa faixa tracejada única ("07/09 a 10/09 · 4 dias livres na cadência"; só o Hoje ganha cartão mesmo vazio); a página de 10 dias cabe numa tela. Hoje ganhou a linha da casa em grade (data · título+chips DEPOIS do título · contato · ações): as três pílulas de reagendar que se repetiam idênticas em toda linha viraram UM menu "adiar ▾" (MenuLinha com prop `gatilho` nova — pill rotulada com chevron) com "+1 dia útil · 02/09", "+3 dias", "próxima semana" dentro; Concluir segue primário. Status: R1 e R2 feitos.
- **Redesign lote 5 (R7+R11, 2026-09-01)**: Demandas virou FILA ÚNICA num cartão só — grupos por estado como cabeçalhos internos (Pendente/Em andamento/…, contagem esmaecida), título na frente com chips depois (o chip de status morreu: o grupo já diz), metadado compacto "por X · 01/09 09:05"; Cancelar foi para o ⋯ (vermelho só na confirmação) e Encerrar virou menu "encerrar ▾" com o formulário (como terminou + resolução) dentro. Configuração saiu do cartão estreito num vão vazio: cartão Marca em cima (2 campos lado a lado) + as três ofertas em cartões próprios lado a lado (lg:grid-cols-3, fio verde no topo, eyebrow humana "Oferta · Nova Receita Digital" — fim do slug vazado nas labels). Status: R7 e R11 feitos (10/19).
- **Redesign lote 6 (R10, 2026-09-01)**: a tela de campanha (a mais velha do console) entrou na linha: título capitalizado no header; a URL do E3 na prévia da copy virou rótulo curto truncado com o endereço completo no title (`CorpoComLinksCurtos` em [slug]/ui-local — visual apenas, o e-mail real segue texto puro); a nota do asterisco de abertura mudou do pé da página para logo abaixo do funil; Últimas atividades agrupadas por dia (cabeçalhos internos com contagem) com "Mostrar mais" via ?limite= (50→500); zebra na tabela de respostas da campanha. T2 concluiu a migração da Campanha (falta só Atividade, em R8). Status: R10 feito (11/19).
- **Redesign lote 7 (R5+R6+R8, 2026-09-01)**: passes finos. Conta do contato: timeline agrupada por dia (cabeçalhos internos como nas demais listas) com a linha mostrando só a hora (title com o carimbo completo), moldura da casa. Pipeline: coluna vazia com direção ("nenhum negócio neste estágio"/"solte aqui") e "0d no estágio" virou "chegou hoje". Atividade: campanha virou o chip-link da casa (mesma âncora de Contatos/Respostas) — com isso o T2 CONCLUIU (todas as tabelas migradas). Status: R5, R6, R8 feitos (14/19). Restam: R9, R12, R13, R14 (passes finos) e T5 (entrada viva).

## Mapa rápido

| Preciso de…                                          | Onde                                                  |
| ---------------------------------------------------- | ----------------------------------------------------- |
| Fonte de verdade                                     | `docs/PRD.md`                                         |
| Regras do agente                                     | `.agents/rules/dreamy-site.md`, `CLAUDE.md`           |
| Plano/fases                                          | `docs/IMPLEMENTATION-PLAN.md`                         |
| Arquitetura                                          | `docs/ARCHITECTURE.md`                                |
| Decisões (ADR-001…024)                               | `docs/DECISIONS.md`                                   |
| Auditoria do site antigo                             | `docs/AUDIT-SITE-ATUAL.md` + `docs/audit/`            |
| Prova/claims permitidos                              | `docs/CONTENT-SOURCES.md`                             |
| Copy autoral a revisar                               | `docs/COPY-REVIEW.md`                                 |
| Tracking/GTM                                         | `docs/TRACKING.md`                                    |
| QA e resultados                                      | `docs/QA.md`, `docs/qa/final/`, `docs/qa/lighthouse/` |
| Migração SEO/domínio                                 | `docs/SEO-MIGRATION.md`, `docs/redirect-map.csv`      |
| Deploy da plataforma (Vercel + Neon + login do time) | `docs/DEPLOY-PLATAFORMA.md`                           |
| Deploy do site (referência; site hoje é do sócio)    | `docs/DEPLOY.md`                                      |
| Apresentação institucional (PDF + fonte HTML)        | `docs/apresentacao/`                                  |
| Campanhas de e-mail outbound (PRD)                   | `docs/PRD-EMAIL-OUTBOUND.md`                          |
| MORK (agente de outbound: identidade + playbook)     | `.agents/mork/MORK.md`, `.agents/mork/PLAYBOOK.md`    |
| Time de agentes do MORK (subagentes)                 | `.claude/agents/` (verbo, garimpo, trato, forja)      |
| Checklist de lançamento                              | `docs/LAUNCH-CHECKLIST.md`                            |
| Variáveis de ambiente                                | `.env.example`, README                                |

## Decisões que ainda precisam de confirmação da Dreamy

1. **Tema**: base clara com seções escuras (ADR-014). Para site 100% escuro: `data-theme="dark"` no `<html>` (`src/app/layout.tsx`) + revisar Hero.
2. **WhatsApp** `+55 11 94879-3233` como padrão (`src/config/site.ts`); CNPJ/razão social/e-mail/endereço/LinkedIn estão `null` (omitidos na UI).
3. **Tipografia** Urbanist + Instrument Sans (ADR-004) — trocar em `src/styles/fonts.ts` se houver fonte oficial.
4. **Copy autoral** e **rascunhos de Insights** (`status: review`, 3 arquivos em `src/content/insights/`) — revisar; publicar exige ≥ 3 `published`.
5. **Políticas** de privacidade/cookies (`src/content/legal/`) — validação jurídica.

## Pendências externas (não bloqueiam código)

- Importar o repositório na Vercel como **plataforma** — `docs/DEPLOY-PLATAFORMA.md` (Neon, envs `OUTBOUND_*`, domínio `mork.bedreamy.com.br`).
- `NEXT_PUBLIC_GTM_ID` + container GTM conforme `docs/TRACKING.md` (GA4 `send_page_view=false`, tag de `page_view` no evento custom, Meta/LinkedIn com consentimento).
- CRM (`CRM_PROVIDER=webhook`, `CRM_WEBHOOK_URL`, `CRM_API_KEY`), e-mail (`EMAIL_PROVIDER=resend` + chave + `LEADS_NOTIFICATION_EMAIL/FROM`), `NEXT_PUBLIC_BOOKING_URL`, Turnstile (opcional), Redis REST (`KV_REST_API_URL/TOKEN`, opcional — rate limit/idempotência globais em serverless).
- Cases aprovados (`approved: true` + `approvalRef` + `docs/CONTENT-SOURCES.md`), logos/métricas autorizados (`src/content/proof.ts`).
- Produção: `NEXT_PUBLIC_SITE_ENV=production` (opt-in explícito — ADR-017), `NEXT_PUBLIC_SITE_URL=https://www.dreamy.app.br`, apex → www no host, Search Console, remover GA4 direto do Framer.

## Próximos passos sugeridos (em ordem)

1. ~~Commit inicial, publicar repositório e conectar CI~~ ✅ (2026-08-18).
2. **Plataforma**: importar o repositório na Vercel pelo painel e seguir `docs/DEPLOY-PLATAFORMA.md` (Storage → Neon, envs, `pnpm outbound:db migrate` + `push`, domínio `mork.bedreamy.com.br`, primeiro login, checklist). Os itens 3–5 abaixo valem só para o site, se ele voltar a ser servido daqui.
3. Preencher variáveis do preview: GTM (validar eventos no Preview do GTM), CRM webhook e Resend com lead de teste; `CONTENT_PREVIEW=true` para revisar insights/cases não publicados.
4. Revisar decisões acima; publicar Insights quando ≥ 3 aprovados; adicionar cases quando aprovados.
5. Avaliar `CSP_ENFORCE=true` após validar tags no browser.
6. **Outbound**: acompanhar o 1º disparo (seg 2026-08-31) e as respostas com o MORK (`.agents/mork/PLAYBOOK.md`); depois do deploy da plataforma, fases P2–P5 do PRD-EMAIL-OUTBOUND §29.3 (Vercel Cron, webhooks + one-click, respostas automáticas, remoção do site deste repo).
7. ~~Fase 8/9 (go-live do site em `www.dreamy.app.br`)~~ — não se aplica mais a este repo (site com o sócio); roteiro mantido em `docs/DEPLOY.md`/`docs/SEO-MIGRATION.md` como referência.

## Armadilhas conhecidas (ambiente/dev)

- Windows/Git Bash: use `MSYS_NO_PATHCONV=1` antes de comandos com rotas iniciando em `/` (ex.: `pnpm tsx scripts/qa-screenshots.ts --routes /contato`).
- Lighthouse local: `CHROME_PATH` apontando para o Chromium do Playwright (`%LOCALAPPDATA%\ms-playwright\chromium-*\chrome-win64\chrome.exe`) e servidor `next start` em 3100 (matar processo anterior na porta antes de rebuild).
- Zod 4: schema do lead usa `zod/mini` + `jitless` (CSP sem `unsafe-eval`); schemas de conteúdo usam `zod` clássico e aceitam datas YAML sem aspas.
- Reveals são CSS + IntersectionObserver (`data-reveal`); sem JS tudo fica visível.
- Git no Windows: `core.autocrlf=true` na máquina; o repositório força LF via `.gitattributes` (`* text=auto eol=lf`) — não "consertar" CRLF/LF à mão. Identidade de commit só neste repositório (`git config user.name/email`, sem `--global`).
- Vercel via integração nesta máquina: `list_*` funciona; `create_git_project` retorna 403 (sem permissão de criar projeto) — usar o painel.
- Dependabot (npm) aplica cooldown de 3 dias (`minimumReleaseAge`): logo após um `pnpm install` com pacotes recém-publicados o job "Dependabot Updates" falha com `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` — transitório, resolve sozinho quando os pacotes envelhecem. Os PRs de bump de actions passam pelo CI completo antes de merge.
- Utilitários: `scripts/dev/ui-shots.ts <pasta> [base]` (banner de cookies, menu mobile, preferências, dropdown), `scripts/dev/form-shots.ts <pasta> [base]` (formulário: erros, passo 2, sucesso), `scripts/dev/deck-shots.ts <html> <pasta> [escala]` (screenshots por slide da apresentação), `scripts/dev/build-deck-pptx.py <pasta-shots>` (PPTX com notas do apresentador; requer `pip install python-pptx`), `scripts/dev/build-deck-native.py [pasta-shots]` (PPTX nativo editável com animações via COM do PowerPoint; requer PowerPoint + `pip install pywin32`), `scripts/dev/pitch-shots.ts <pasta>` (screenshots da apresentação HTML viva), `scripts/dev/overflow-check.ts`, `reveal-check.ts`, `csp-check.ts`, `axe-check.ts` (dev), `pnpm qa:screenshots --viewports … --no-shots` (só checagens), `pnpm smoke --base <url>` (deploy).
