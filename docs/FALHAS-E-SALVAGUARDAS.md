# Falhas e salvaguardas — registro permanente da operação de outbound

Ordem do Luigi (2026-09-10): "procurar e corrigir potenciais falhas; se encontrar, registrar para que nunca mais ocorram". Este arquivo é o registro. Regra: **toda falha entra aqui com causa, correção e a salvaguarda que a impede de voltar** (teste, configuração, checklist ou automação). Falha sem salvaguarda não está corrigida.

Convenção de status: `corrigida` (salvaguarda no lugar) · `mitigada` (procedimento manual) · `aberta`.

| #   | Data       | Falha                                                                                                                                                                         | Causa raiz                                                                                                                              | Correção                                                                                                                                                                      | Salvaguarda ("nunca mais")                                                                                                                                                      | Status    |
| --- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 1   | 2026-09-04 | Ciclo diário não disparou 03 e 04/09.                                                                                                                                         | Tarefa `DreamyOutboundAuto` configurada para não iniciar na bateria; sem "iniciar quando disponível".                                   | `Set-ScheduledTask` com AllowStartIfOnBatteries + DontStopIfGoingOnBatteries + StartWhenAvailable.                                                                            | Checklist de pré-voo (PLAYBOOK): conferir `Get-ScheduledTaskInfo` (LastTaskResult 0, próxima execução) toda segunda.                                                            | corrigida |
| 2   | 2026-09-07 | Migração do e-mail para o Google deixou `contact@` inexistente; respostas de prospects bounceavam no remetente.                                                               | MX trocado sem a caixa/alias existir no Workspace.                                                                                      | Caixa/alias criados; MX só Google; SPF `include:_spf.google.com`; DKIM `google._domainkey`; supressão do Resend limpa via API.                                                | `scripts/dev/test-recebimento.ts <caixa>` (entrega real) faz parte de qualquer mudança de DNS/e-mail; nunca trocar MX sem rodar o teste nas duas caixas.                        | corrigida |
| 3   | 2026-09-07 | Lock "outbound" órfão travava a fila por 10 min (4 ocorrências).                                                                                                              | `releaseDbLock().catch(() => {})` engolia falha de rede; `process.exit` dentro do lock pulava o finally.                                | `releaseDbLockReliably` (retry + warn) e `stealDeadLocalDbLock` (pid morto do mesmo host).                                                                                    | Unit em `tests/unit/outbound-store-pg.test.ts` (release com falha transitória; órfão removido). Diagnóstico: `scripts/dev/dump-lock.ts`.                                        | corrigida |
| 4   | 2026-09-07 | Ciclo enviou em feriado nacional (Independência).                                                                                                                             | Janela só conhecia sábado/domingo.                                                                                                      | Tabela de feriados nacionais 2026–2027 no motor; plano e Agenda tratam como fim de semana.                                                                                    | Unit: 07/09/2026 bloqueado, 08/09 liberado, 25/12 bloqueado. Renovar a tabela todo dezembro (item do PLAYBOOK).                                                                 | corrigida |
| 5   | 2026-09-10 | Todo follow-up atrasava um dia (e pulava fim de semana): E2 de 03→04/09; E3 cairia de sexta para segunda.                                                                     | Cadência comparava `lastSendAt + offsetDays` com o instante do ciclo (09:05); o goteo envia à tarde, então o passo só "vencia" à tarde. | Cadência por data-calendário local (`dataLocal(now) >= dataLocal(lastSendAt) + offsetDays`); Agenda prevê igual.                                                              | Unit no engine e na agenda (E2 enviado 04/09 17:17 vence no ciclo de 07/09 09:05). Gatilhos triplos (#6) como segunda linha.                                                    | corrigida |
| 6   | 2026-09-10 | Máquina desligada de manhã = dia sem envio (09/09 rodou às 18:00, fora da janela).                                                                                            | Tarefa com um único gatilho (09:05).                                                                                                    | Três gatilhos seg–sex: 09:05, 12:05, 15:05 (plano idempotente; `MultipleInstances=IgnoreNew`).                                                                                | Pré-voo semanal confere os 3 gatilhos. Idempotência do plano testada em `outbound-engine.test.ts`.                                                                              | corrigida |
| 7   | 2026-09-10 | Grafias diferentes da mesma empresa (AGS ×3, Pierserv/PierServ, BLS/BLS Tech, PELOG/Pelog, SPARE/Spare, Furlong ×2) quebravam a frase dos colegas e o agrupamento.            | Clay exporta o nome da empresa como está em cada perfil; `normalizeEmpresa` só normaliza sufixo/acentos, sem fuzzy (por segurança).     | `scripts/dev/canonizar-empresas-logistica.ts` aplica a grafia canônica da auditoria (grafia original em `custom.empresa_grafia_clay`). Solos: 13 → 8 (as 8 de contato único). | Checklist de importação (PLAYBOOK): auditoria do GARIMPO unifica grafias ANTES da inscrição; `apply-colegas --dry-run` tem que bater com o número de empresas de contato único. | corrigida |
| 8   | 2026-09-10 | Três contatos da cesta com e-mail de OUTRA empresa (headline aponta emprego novo): o E1 diria "aí na Smolka" para alguém da Quality Transportes.                              | Lista do Clay com dado desatualizado; cesta aceitou com alerta.                                                                         | `scripts/dev/segurar-dominio-divergente.ts`: enrollment parado, contato na reserva (`dominio-divergente-logistica`). Cesta 69 → 66.                                           | Regra da auditoria: e-mail com domínio divergente do da empresa NUNCA entra na onda 1 (vai à reserva até confirmação manual).                                                   | corrigida |
| 9   | 2026-09-10 | `{{frase_colegas}}` estática (gravada na inscrição) podia citar colega que saiu da sequência antes do E1; variante solo ("quem sente isso na ponta") errada para diretor/CEO. | Frase calculada uma vez, fora do momento do envio.                                                                                      | Frase calculada NO ENVIO a partir do plano do dia (colegas que realmente saem juntos); variante solo neutra; módulo compartilhado `colegas.ts`.                               | Unit no caminho de envio (grupo de 3 → 2 nomes; colega fora do plano → solo; template sem a variável → intacto).                                                                | corrigida |
| 10  | 2026-09-10 | Copy editada depois do approve bloqueia a campanha em silêncio (o motor exige novo approve e só registra skip por inscrito).                                                  | Invalidação do hash é correta, mas sem alerta agregado.                                                                                 | Alerta destacado no plano e no `outbound:auto` + evento `outbound.plan.bloqueio_aprovacao`.                                                                                   | Unit do computePlan com o motivo agregado. Regra do PLAYBOOK: toda edição de copy termina com `campaign approve --by "Luigi Choffe"` registrado por escrito pelo Luigi.         | corrigida |
| 11  | 2026-09-07 | Resumo de IA do Clay ("Summarize LinkedIn profile") inutilizável para personalização.                                                                                         | Export do Clay trunca a coluna em 100 caracteres.                                                                                       | Personalização passou a usar `summary` (Sobre) + `headline`.                                                                                                                  | Checklist de importação: conferir comprimento máximo das colunas de texto no dry-run antes de contar com elas.                                                                  | mitigada  |
| 12  | 2026-09-10 | Slots de personalização longos estouram o limite de 140 palavras do E1 e o lint barra o envio.                                                                                | Briefing inicial permitia 40/60 palavras; corpo fixo + assinatura já consomem ~100.                                                     | Limite de 22 palavras por slot; validador `merge-personalizacao.ts` renderiza o E1 real com a variante mais longa da frase dos colegas.                                       | Validador tudo-ou-nada antes de qualquer merge; limite documentado no cabeçalho da campanha.                                                                                    | corrigida |

## Pré-voo semanal (segunda, antes das 09:05) — MORK

1. `Get-ScheduledTask DreamyOutboundAuto | Get-ScheduledTaskInfo`: LastTaskResult 0, próxima execução hoje, 3 gatilhos, AllowStartIfOnBatteries.
2. `pnpm outbound:arm status`: armada, breaker ok, Reply-To presente (2 endereços).
3. `pnpm outbound:plan`: sem alerta de aprovação; grupos por empresa inteiros; nenhum feriado esquecido.
4. `pnpm tsx scripts/dev/dump-lock.ts`: nenhum lock órfão.
5. `pnpm tsx scripts/dev/test-recebimento.ts contact@bedreamy.com.br` (só após mudança de DNS/e-mail).

## Checklist de importação de lista nova — GARIMPO + MORK

1. `outbound:import --dry-run`: conferir colunas que caíram em `custom`, comprimento das colunas de texto, duplicados.
2. Auditoria do GARIMPO ANTES de inscrever: ICP de empresa, cargos, **grafias unificadas**, **domínio divergente = reserva**, cap por empresa.
3. Verificação de entregabilidade (externa ou decisão escrita do Luigi).
4. Peças personalizadas pelo validador (tudo ou nada) → inscrição cirúrgica → `apply-colegas --dry-run` bate com as empresas de contato único → `lint-campanha-alvo` 100% → `render-amostras` para o Luigi → approve por escrito.
